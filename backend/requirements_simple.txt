# PRALAYA-NET Backend - Essential Dependencies Only
# Optimized for Python 3.12 with modern package versions
# Fixed for Render deployment - build tools at top

# ⚠️ CRITICAL: Build system MUST be at the top and upgraded first
# This fixes the "BackendUnavailable: Cannot import 'setuptools.build_meta'" error
setuptools>=69.0.0
wheel>=0.42.0
pip>=24.0

# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# HTTP client and async support
httpx>=0.26.0
aiofiles>=23.2.1

# Environment configuration
python-dotenv>=1.0.1

# Data validation and serialization
pydantic>=2.5.3

# WebSocket support
websockets>=12.0

# Data Science (Python 3.12 compatible)
numpy>=1.26.0
networkx>=3.3

# Optional: For enhanced features - uncomment if needed:
# requests>=2.31.0
# pillow>=10.1.0
# python-jose[cryptography]>=3.3.0
# passlib[bcrypt]>=1.7.4

