# PRALAYA-NET Environment Configuration
# Copy this file to .env and fill in your actual values

# ============ BACKEND ============
# Backend server configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
BACKEND_RELOAD=true

# ============ FRONTEND ============
# React dashboard URLs
REACT_APP_API_URL=http://localhost:8000
REACT_APP_WS_URL=ws://localhost:8000

# ============ ESP32 HARDWARE ============
# ESP32 controller configuration
ESP32_HOST=192.168.1.100
ESP32_PORT=80
ESP32_WIFI_SSID=YOUR_WIFI_SSID
ESP32_WIFI_PASSWORD=YOUR_WIFI_PASSWORD

# ============ GOVERNMENT/OPEN DATA APIS ============
# NASA and Government data API keys
# Get your API key from: https://api.data.gov/
# Never commit this file with real keys to version control
DATA_GOV_KEY=YOUR_API_KEY_HERE

# NASA Earth Imagery API
NASA_API_KEY=YOUR_NASA_API_KEY_HERE

# ============ DATABASE (OPTIONAL) ============
# For future database integration
DATABASE_URL=sqlite:///./pralaya.db

# ============ SECURITY ============
# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173

# Rate limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=100
RATE_LIMIT_ENABLED=true

# ============ LOGGING ============
LOG_LEVEL=INFO
LOG_FILE=logs/pralaya.log

# ============ DEPLOYMENT ============
# Set to 'production' when deploying to Render/Vercel
ENVIRONMENT=development

# Render deployment URL (for production)
RENDER_EXTERNAL_URL=https://your-render-app.onrender.com

# Vercel frontend URL (for production)
VERCEL_FRONTEND_URL=https://your-vercel-app.vercel.app
